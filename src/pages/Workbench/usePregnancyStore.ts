// import { useEffect } from "react";
// import { BedStatus } from "@lianmed/lmg/lib/services/types";

// const { Working,Offline, Stopped } = BedStatus;

// export default (status: BedStatus, name: string, age: string) => {
//     useEffect(() => {
//         if ([Working]){
//             const _all = sessionStorage.getItem('bed');
//             let all = _all ? JSON.parse(_all) : {};
//               // 停止监护
//               all[bedname] = pregnancy;
//               const bedString = JSON.stringify(bed); // JSON.parse(str)
//               sessionStorage.setItem('bed', bedString);
//         }
//     }, [status])
// }